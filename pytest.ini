[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --tb=short
    --strict-markers
    -v
markers =
    slow: tests yang membutuhkan waktu lama (lewati dengan -m "not slow")
    integration: tests integrasi end-to-end
